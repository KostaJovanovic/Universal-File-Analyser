# HTML / serve.py / sw.js / tools comment audit

## index.html — no issues

All HTML comments and inline `<script>` comments (offline-toggle default-open behaviour, mobile-only `[?]` help, the formats-overlay "generated from formats.js by app.js" note, the FOOTER markers, the "no address in source / Turnstile" note, and the service-worker auto-reload + `updateViaCache:'none'` comments) accurately match the adjacent markup and code. (The `740` in `data-fmt-count="bare">740` is stamped at runtime, not a comment.)

## about.html — no issues

The two content comments are correct: line 198-199 ("Inline list kept for SEO + #fmt-/#ext- deep-links. Generated from assets/js/core/formats.js by app.js (renderAboutFormats).") and line 206-207 ("Same supported-formats popup as index.html ... Generated by app.js (renderFmtOverlay).") both match how `#aboutFormats` / `#fmtOverlay` are populated. Footer/SW comments are the shared, accurate ones.

## patch.html

- **Line 107** — `"Commit #29 was crowned 1.0 (see RELEASE_COMMITS in app.js + RELEASE in save.bat, ...)"` → the save.bat constant is named `RELEASES` (plural), not `RELEASE` (`set RELEASES=29,60,100` at save.bat:53; CLAUDE.md also calls it `RELEASES`). **Fix:** "... see RELEASE_COMMITS in app.js + RELEASES in save.bat ...".
- **Line 109** — `"To crown a future 2.0, add that commit number to RELEASE_COMMITS."` → stale: 2.0 (and 3.0) already exist. `RELEASE_COMMITS = [29, 60, 100]` in app.js (commit 29 = 1.0, 60 = 2.0, 100 = 3.0), and the page already carries 2.0/3.0 milestone entries. The next crownable release is 4.0. **Fix:** "To crown a future 4.0, add that commit number to RELEASE_COMMITS." (and likewise line 120's "start a new group above it ... Keep the 1.0 and 2.0 milestones" describes the digest convention and is still fine, but the "future 2.0" wording is the stale part).

Note: the VERSIONING SCHEME paragraph's worked examples (0.01 … 1.0 after commit 29) remain correct; only the "future 2.0" forward-looking instruction is out of date. The "EXACTLY the latest 3 entries stay visible" rule (line 116) matches the page (3 `.patch-entry` before `<details class="about-formats">`), and the PATCH_DIGEST / tl;dr references are accurate (PATCH_DIGEST exists in app.js).

## stats.html — no issues

Footer toggle, mobile `[?]`, FOOTER markers, Turnstile-mailto note and SW comments all accurate.

## privacy.html — no issues

Same shared, accurate footer/SW comments.

## formats.html — no issues

- Line 100-104 ("The list below is GENERATED from assets/js/core/formats.js by tools/prerender-formats.mjs (run by save.bat) ... edit the catalog in formats.js. The #fmt-<slug> / #ext-<ext> ids are the deep-link anchors ...") correctly describes the `<!-- FORMATS:START -->`/`END` region and the generator.
- Line 1104-1106 ("Same searchable supported-formats popup ... data-fmt-open opens it; app.js fills #fmtBody via renderFmtOverlay()") is accurate.

## atari.html

- **Line 84** — `"<!-- FOOTER:START - generated by tools/stamp-footer.mjs from tools/partials/footer-shared.html; do not hand-edit between the markers -->"` → misleading on this page: atari.html is **not** in stamp-footer.mjs's `PAGES` array (`['index.html', 'about.html', 'patch.html', 'stats.html', 'privacy.html', 'formats.html']`), so the generator never stamps it. The footer block here is in fact stale/divergent from the partial (e.g. it still shows the old "48 MB / 72 MB / 310 MB" tier sizes and "1017+ file formats" while the stamped pages show "50 MB / 78 MB / 325 MB", and it omits the Photoshop/Fonts/E-books/Spreadsheets/DWG dependency rows the partial now contains), confirming it is not generated by the script. **Fix:** either add `atari.html` to `PAGES` in stamp-footer.mjs so the marker's claim is true, or, if the page is intentionally excluded, drop the "generated by tools/stamp-footer.mjs ... do not hand-edit" wording from its marker.
- **Line 67-69** — `"Dev-only hard reload (clears the cache so code edits show up) ... Revealed by app.js only on dev hosts; stays hidden in production."` → accurate (button is `hidden` and gated to dev hosts). No issue.

## serve.py — no issues

Docstring and inline comments are accurate: clean-URL routing (`/about.html` → 308 → `/about`, `/` → index.html, SPA fallback), the MOCK_STATS rationale (no Worker/D1 under server.bat), the legacy no-meta score rows, the `_serve_api` mock endpoints, port 3000 / `0.0.0.0` bind, and the ThreadingHTTPServer deadlock-avoidance note all match the code. (serve.py's own 308 is internally consistent in comment and code; the "307" figure that disagrees lives in wrangler.jsonc, which is outside this audit's scope.)

## sw.js — no issues

`VERSION = 'analyser-v132'`; the DEV pass-through comments, the `allSettled` per-entry-precache rationale (replacing `cache.addAll`'s all-or-nothing), the Turnstile best-effort `no-cors` precache note, and the `/api/*` "always live, never cached" comment all match the implementation.

## tools/prerender-formats.mjs — no issues

Header WHY/HOW/RUN block and inline comments are accurate: rewrites only the `<!-- FORMATS:START -->`/`END` region and the `<!--FMTCOUNT-->` count, reads `catalogGrouped()`/`formatCount()` from formats.js, emits fmtItem-shaped markup, run by save.bat. The full-vs-id routing comment matches `buildFullKeys`/`makeHrefOf`.

## tools/prerender-format-pages.mjs — no issues

WHAT/WHY/SCOPE/CONTENT/OUTPUTS header matches the code: one file per extension, `/formats/<ext>` for full and `/formats/id/<ext>` for id (full-wins on collisions, e.g. the `.h264` example), copy from format-page-content.mjs with a WARNING + generic fallback for missing full-analysis exts, outputs to `formats/`, `formats/id/`, and `sitemap-formats.xml`. The siteNav, capabilityBlocks, assembleFacts and EXTRA_FACTS sidecar comments all match their code.

## tools/prerender-common.mjs — no issues

Header and the `buildFullKeys` / `makeHrefOf` comments correctly describe the shared escaping helpers and the full→`/formats/<ext>` vs id→`/formats/id/<ext>` routing.

## tools/stamp-counts.mjs — no issues

Header WHY/RUN and the JOBS table comments are accurate: stamps the live `formatCount()` into index.html/patch.html/manifest.json count strings and refreshes sitemap.xml `<lastmod>`, idempotent regexes, run by save.bat. The "200+ proprietary ... left alone" caveat matches (llms.txt/README are not in JOBS).

## tools/stamp-footer.mjs — no issues

Header and inline comments match: stamps the shared block between `FOOTER:START`/`FOOTER:END` from `tools/partials/footer-shared.html`, `PAGES` excludes the per-format pages, leaves `.footer-bottom` per-page, warns on missing markers. (The atari.html mismatch is an issue in atari.html's marker comment, not in this generator — see above.)

## tools/format-page-content.mjs — no issues

The header comment ("Per-extension copy for the /format/<ext> landing pages", consumed by prerender-format-pages.mjs; full-analysis exts SHOULD have an entry and are WARNED if missing; IDENTIFICATION_CORE entries present, EXTENDED fall back without warning; `{ name, blurb, fact?, factApprox? }` shape) accurately describes the exported `EXT_PAGES` map and how the generator uses it.
