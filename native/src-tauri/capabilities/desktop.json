{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Auto-updater permissions. Scoped to desktop platforms because tauri-plugin-updater has no mobile build - matches the #[cfg(desktop)] gating in lib.rs and the desktop-only dependency in Cargo.toml. Without this scoping the Android build script aborts on an unknown 'updater:default' permission.",
  "platforms": ["macOS", "windows", "linux"],
  "windows": ["main"],
  "permissions": [
    "updater:default"
  ]
}
