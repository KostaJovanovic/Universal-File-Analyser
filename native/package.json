{
  "name": "analyser-native",
  "version": "0.1.0",
  "private": true,
  "description": "Tauri shell for the Analyser native app (desktop + mobile). The website itself, in the repo root, stays dependency-free.",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "dist": "node build-dist.mjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  }
}
