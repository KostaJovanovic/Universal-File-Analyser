# Local research notes — not part of the shipped site
*-RESEARCH.md

# Local research / reverse-engineering scratch - not shipped, not tracked
research/
research2/

# Python bytecode cache (serve.py) - generated locally, never shipped
__pycache__/

# Wrangler local cache / temp (generated by `wrangler dev` / `wrangler deploy`)
.wrangler/

# Local stats backup snapshots (tools/backup-stats.mjs) - kept off-repo
stats-backup/

# Standalone minimal build - destined to be its own repo
minimal/
nul

# Local docs work-order / planning note - not shipped, not tracked
DOCS_PLAN.md

.codex/

# Claude Code: skills and project settings ARE checked in (they document the
# repo's workflows); only the machine-specific local settings stay out.
.claude/settings.local.json

# README media staging - scratch working folder, not shipped
README TEMP/
graphify-out/

# npm: exists only so tsc can build src/*.ts -> web/assets/js/*.js.
# The compiled output IS committed (like web/formats/ and web/docs/) so the
# Cloudflare deploy stays a pure static upload of web/ with no build step.
node_modules/
