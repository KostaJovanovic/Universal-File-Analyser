<?xml version="1.0" encoding="utf-8"?>
<!-- Android 12 and later. Nothing leaves the phone: not to a Google Drive
     backup, and not to a new phone in a device transfer. allowBackup="false"
     in the manifest stops the cloud backup, but device transfer ignores it, so
     every domain is excluded here as well. The WebView's storage (the
     "Recently analysed" list - file names, sizes, types), the offline cache,
     ffmpeg sessions and staged saves all live in these domains. -->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
        <exclude domain="device_root" path="." />
        <exclude domain="device_file" path="." />
        <exclude domain="device_database" path="." />
        <exclude domain="device_sharedpref" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
        <exclude domain="device_root" path="." />
        <exclude domain="device_file" path="." />
        <exclude domain="device_database" path="." />
        <exclude domain="device_sharedpref" path="." />
    </device-transfer>
</data-extraction-rules>
